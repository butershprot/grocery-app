export function Input(props) {
  return (
    <input
      {...props}
      style={{
        border: "1px solid #ccc",
        borderRadius: "4px",
        padding: "8px",
        marginRight: "4px"
      }}
    />
  );
}

